using UnityEngine;

public class sparkleaction : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Creating sparkle object.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
